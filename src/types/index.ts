export interface Service {
  title: string;
  description: string;
  icon: string;
}

export interface PhilosophyItem {
  title: string;
  description: string;
  icon: string;
}

export interface FormData {
  name: string;
  email: string;
  phone: string;
  message: string;
}